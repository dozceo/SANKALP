# SANKALP AEI - Python ML Dependencies
# Ensemble Predictive Modeling stack

lightgbm>=4.6.0
xgboost>=2.0.0
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.12.0
# torch is optional — DNN training falls back gracefully when PyTorch is unavailable
torch>=2.0.0
# TensorFlow — primary DNN for mastery prediction (.h5 Keras models)
tensorflow>=2.14.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
httpx>=0.27.0
joblib>=1.3.0
# ONNX conversion (LightGBM/XGBoost → ONNX for edge deployment)
onnxmltools>=1.12.0
