# Database Schema Migration Risk Analysis

Generated by: `scripts/analyze-schema-risks.ts`

## ML Model Feature Requirements
derived from `src/ml/training/training_data.csv` headers
- avg_quiz_score
- attempts_per_topic
- days_since_last_revision
- quiz_score_variance
- time_spent_per_question
- mastered

## Current Frontend Data Schema
derived from `src/types/intelligence.ts` (MasterySignal interface)
- score
- confidence
- daysSinceRevision
- attempts
- trend
- needsRevision
- priority

## Gap Analysis & Risk Assessment
| ML Feature | Schema Field | Risk Level | Mitigation |
|---|---|---|---|
| `avg_quiz_score` | Mapped to `score` | **LOW** | Ensure name transformation logic (snake_case to camelCase) is implemented correctly. |
| `attempts_per_topic` | Mapped to `attempts` | **LOW** | Ensure name transformation logic (snake_case to camelCase) is implemented correctly. |
| `days_since_last_revision` | Mapped to `daysSinceRevision` | **LOW** | Ensure name transformation logic (snake_case to camelCase) is implemented correctly. |
| `quiz_score_variance` | MISSING | **HIGH** | Must include this column in new DB schema and ensure data collection pipeline populates it. |
| `time_spent_per_question` | MISSING | **HIGH** | Must include this column in new DB schema and ensure data collection pipeline populates it. |
| `mastered` | Mapped to `needsRevision` | **LOW** | Ensure name transformation logic (snake_case to camelCase) is implemented correctly. |

## Critical Findings

**CRITICAL RISK**: The following ML features are MISSING from the current frontend/type schema:
- `quiz_score_variance`
- `time_spent_per_question`

These fields must be persisted in the database schema (e.g., in a `TopicAttempt` or `StudentMastery` table) to support future model retraining. If they are not stored, the ML model cannot be retrained effectively.

## Backward Compatibility Check
- **Synthetic Data**: The pending DB migration must ensure that `training_data.csv` can be loaded or replicated.
- **Rollback Safety**: If migration fails, ensure the app can fallback to `mock-db.ts` which uses `src/types/intelligence.ts`. The missing fields suggest `mock-db.ts` might be insufficient for *training* but fine for *inference* if the model handles missing features (imputation) or if the model is only used for prediction on features available at runtime.
